# Progress

- 2026-07-22: Preferred-title parser hardening (ad-hoc Phase-9.5 reliability fix) — added an endpoint-level review-save persistence regression, filtered bare generic model output such as “Principal” or “Engineer”, and made generic-only output a successful empty review instead of a false 400; 19 focused title tests pass.
- 2026-07-22: Fixed silent skill-deactivation bug (ad-hoc, found via live-search skills-overlap-score-stuck-at-0 investigation) — `routes/skills.py`'s overwrite-mode parse-preview endpoint no longer deactivates+commits existing `CandidateSkill` rows before user confirmation; deactivation moved into `save-parsed`, gated on a `parse_mode` field now passed from `parse_result.html`, so a discarded/abandoned preview can no longer wipe live skill data. 243/243 tests pass. Candidate's existing 58 deactivated skill rows still need a maintainer-directed restore (re-parse or other source) — not yet done.
- 2026-07-23: Ad-hoc architecture review + fix (not a roadmap slice) — ran `/improve-codebase-architecture` against backend hot spots, producing 5 deepening candidates (report: `/tmp/architecture-review-20260723-220236.html`, not committed). Maintainer picked candidate 2: `JOB_ANALYSIS_PROMPT` in `services/job_analysis.py` hardcoded one candidate's fixed profile (Armenia/EDA/VLSI/Staff-Principal-SDET) into every job-match score regardless of which candidate was being searched for — a real correctness bug, not just a cleanliness issue. Fixed by templating the prompt's profile section from fields already available at the call site (`candidate.location`, `.timezone`, `.experience_years`, `.current_role`, active `CandidateJobTitle` rows) rather than passing the ORM object itself, matching the existing `candidate_skills: List[str]` parameter style; extended the on-disk cache key to include the candidate profile identity so different candidates no longer collide on cache (callers that omit the new fields keep the old key, so nothing else broke). Added 6 new tests to `test_job_analysis.py` (template has no hardcoded candidate, prompt reflects given profile, falls back to "Not specified", cache-key differentiation). 22/22 `test_job_analysis.py` tests + 7 `job_search`-adjacent tests pass. The other 4 candidates from the review (dual LLM provider-config systems, `JobSearchService.search()` god-method, scraper duplication, `routes/jobs.py` handler triplication) remain open, undecided, not yet ADR'd. Uncommitted.
- 2026-07-23: Second ad-hoc architecture review pass (separate from the one above, same day) — ran `/improve-codebase-architecture` again against `backend/services/`, producing a fresh set of 5 candidates (report: local temp HTML, not saved in-repo). Grilled and implemented candidate 2: added `job_deduplication.from_scraped_dict(job_data: dict) -> Job` as the single owner of the scraper-dict→`Job` comparison shape used for dedup matching; `job_search.py`'s `_plan_run_memberships` now calls it instead of two inline field-mapping blocks (497-500, 506-509); deleted the dead, unreferenced `_deduplicate_jobs` method (superseded earlier by `_plan_run_memberships`, never removed — confirmed no call sites, no test references). Added 3 tests to `test_job_deduplication.py`. 255/255 tests pass. Uncommitted. Remaining 4 candidates from this pass (search-run diagnosis spread across 4 state channels, `analyze_job`'s 10-param shallow interface, splitting `job_title_parser.py` into extraction vs. reconciliation modules, `search_lock` enforcement living only in the route layer) deferred, undecided, not yet ADR'd.

- 2026-05-30: Workflow bootstrap — linked shared agent controls and created local workflow state files for `/session-open`.
- 2026-05-30: AI migration direction captured — documented the `remote-rover`-derived provider/chat/agent architecture as the canonical path for `job-finder`.
- 2026-05-30: Grill session finalized — promoted the AI migration outcome into ADR 0001 for durable implementation guidance.
- 2026-05-30: Port the `remote-rover` provider settings backend/UI into `job-finder` as the canonical LLM system — added canonical AI config/secret stores, JSON APIs, and the first API-driven provider settings page.
- 2026-05-31: Reconnected `/api/chat` to persisted AI sessions — chat turns now save in `AISessionStore`, reload by `session_id`, and are covered by focused route tests.
- 2026-05-31: Exposed saved AI sessions in `/chat` — the UI now lists, reloads, and switches persisted transcripts while preserving a quick new-chat reset.
- 2026-05-31: Added active-session management controls in `/chat` — saved chats can now be renamed or archived directly from the session sidebar.
- 2026-05-31: Added per-message retry in `/chat` — assistant turns can now replay the preceding user prompt through the active persisted session.
- 2026-05-31: Added streamed chat transport in `/api/chat/stream` — assistant chunks now stream to the client while the final reply is still persisted into the saved session.
- 2026-05-31: Switched `/chat` to streamed assistant rendering — the chat UI now consumes the stream incrementally and finalizes saved-session state from the terminal event.
- 2026-05-31: Added focused streaming chat coverage — route tests now assert stream session events, final persisted transcripts, timeout handling, and saved-session reuse.
- 2026-05-31: Added per-session source controls in `/chat` — saved sessions now expose editable source toggles that persist through the AI session API.
- 2026-05-31: Added candidate attachment to AI sessions — saved chats can now attach one candidate, gate candidate-only sources, and send attached candidate context through chat requests.
- 2026-05-31: Locked job-finder AI routing/capability contract — added canonical routing + capability constants, normalized provider capabilities, enforced session mode whitelist, and surfaced routing/capability metadata via AI settings APIs/UI.
- 2026-05-31: Implemented initial job-finder AI tool surfaces — added `/api/ai/tools` registry + execution adapter for candidate/session/settings lookups and workspace read/write with mutation-policy gating.
- 2026-05-31: Repointed chat execution to AI routing config — `/api/chat` and `/api/chat/stream` now resolve provider/model via `model_routing.general_chat` with fallback coverage in route tests.
- 2026-05-31: Repointed document and candidate analysis to routing-purpose selection — `document_parser`, `job_analysis`, and skill extraction now use shared `document_analysis` / `candidate_analysis` routing with fallback-safe behavior and updated job-analysis coverage.
- 2026-05-31: Add job/search-result surfaces and employer-facing context — expanded `/api/ai/tools` with candidate job and employer summary lookups plus focused route tests.
- 2026-05-31: Add approval-aware mutation flows for non-file writes — added write-tool approval handshake on `/api/ai/tools/execute` with approval-required/approved path coverage.
- 2026-05-31: Expand agent mode beyond the initial toolset — added candidate application lookup tool plus bounded limit validation coverage for AI tool execution routes.
- 2026-05-31: Port the session-based AI chat shell with persisted sessions, streaming, retry, rename, archive, and provider override — added explicit `/api/chat` and `/api/chat/stream` routing-override tests and closed the Phase 1 parent checklist item.
- 2026-05-31: Align the AI chat shell with `remote-rover` UX, including organized session/control layout and Enter-to-send composer behavior — consolidated `/chat` layout, restored Enter-to-send behavior, and added startup schema/logging fixes for stable chat loading.
- 2026-05-31: Consolidated remote-rover parity gap review handoff — merged full LLM providers + AI chat missing-feature findings into `handoff-remote-rover-gap-review-2026-05-31.md`.
- 2026-05-31: Add ordered fallback routing editor for `fallback_provider_ids` in LLM settings — routed per-purpose fallback ordering now supports add/remove/reorder in UI and persists through `/api/llm-settings`.
- 2026-05-31: Add provider quick actions in settings table — implemented row-level enable/disable, provider health check probe, and set-default action for `general_chat` in `llm.html`.
- 2026-05-31: Complete Phase 4 Remote-Rover Parity P0 (Core Controls) — added chat stop-stream (`AbortController` + streaming-only stop button) and archived session sidebar management with active/archived filtering and search.
- 2026-05-31: Landed three Phase 5 parity slices — verified runtime-override routing toggle coverage, expanded provider templates, and added chat run-mode controls (`chat`/`agent`/`intent`/`planning_shell`) wired through chat APIs/session mode.
- 2026-05-31: Complete Phase 5 Remote-Rover Parity P1 — added slash command local fallbacks, stream trace/tool activity + diagnostics blocks, reload recovery hinting, and suspended-run clarification cards in chat stream UX.
- 2026-06-05: Add typed read-only tool definitions for `list_data_surfaces` and `candidate_skills_lookup` — added the `backend.ai_agent` schema/registry layer with validation tests.
- 2026-07-19: Applied recommended answers — Q8 groundwork-now approved, P1–P5 extensions approved into R3/R7/R11 + roadmap 9D/9E/9F, legacy scripts archived to `history/legacy-scripts/`.
- 2026-07-19: Resolved Q6 — per-model capabilities, verification probe, route-first/degrade-visibly agent fallback (remote-rover pattern reviewed and improved) in `docs/design/agent-provider-capabilities.md`; open questions now zero.
- 2026-07-19: Plan-review triage complete — all 9 GPT-5 findings accepted and fixed at doc level: 4 design contracts (scoring/provenance rework, search-runs+snapshot lists, 9B ownership groundwork, remote verification), roadmap 9B/9C/9D/10 slices updated, review file marked RESOLVED.
- 2026-07-19: R2 profile-card pre-review — audited all profile-page cards and recorded reproducible P0–P2 behavior divergences and the shared target pattern.
- 2026-07-19: Phase-10 ownership groundwork — added a seeded development principal, candidate ownership boundary, legacy backfill migration, and regression coverage.
- 2026-07-19: R4 platform research — ranked platform options and selected We Work Remotely as the next post-LinkedIn integration target.
- 2026-07-19: Roadmap restructured into three parallel non-blocking threads (T1 profile/UI, T2 data model/pipeline, T3 platforms) — one cross-thread edge (T1 provenance needs T2 groundwork) neutralized by slice ordering.
- 2026-07-19: R2 app-wide UI/UX cross-comparison — recorded the cross-page pattern inventory and target pattern set for future UI slices.
- 2026-07-19: R5 deterministic title-match scoring — added alias-aware persisted title scores, best-match ordering, and regression fixtures.
- 2026-07-19: R3 LinkedIn login reliability preparation — added a pre-search session probe with explicit failure reasons and focused coverage; guided UI/manual validation remains HITL.
- 2026-07-19: R2 shared profile cards — unified preferred titles and skills with consistent collapsible previews, actions, and shared styling/behavior.
- 2026-07-19: R5 weighted composite scoring — added title/skills weights, alias-safe skill matching, persisted breakdown/version, and regression coverage.
- 2026-07-19: R1 extraction provenance — recorded per-document extraction occurrences, migrated legacy source links, and added extracted/edited/reset controls to profile cards.
- 2026-07-19: R5 LLM top-N refinement — added opt-in, idempotent candidate-analysis refinement that persists companion score/rationale without changing deterministic ranking.
- 2026-07-19: R1 content-aware document classification — classified resume/CV content independently of misleading filenames and retained Other for non-resume documents.
- 2026-07-19: R6 remote-status verification — persisted canonical verified-remote facts with restrictions/evidence/version, unknown fallback, and mocked contradiction coverage.
- 2026-07-19: R7 results-table upgrade — added sortable salary, verified-remote, and score columns with best-match ordering and expandable score details.
- 2026-07-19: R10 profile visibility — added draft/private/public status, legacy migration, validation, profile UI, and regression coverage.
- 2026-07-19: R7 saved search lists — added named historical search snapshots and sightings-based deduplication for known jobs.
- 2026-07-19: R9 profiles-per-user UX — scoped “My Profiles” to the current user with ownership enforcement and per-profile summaries.
- 2026-07-19: R3 WWR adapter foundation — added a policy-gated, fixture-tested provider-neutral adapter; live authorization and search wiring remain.
- 2026-07-19: R7 result curation — added non-destructive dismissal, score/remote filters, and filtered saved-list CSV exports.
- 2026-07-19: R3 WWR search wiring — made the policy-gated adapter selectable and fail closed without live provider access.
- 2026-07-19: R3 LinkedIn login recovery — added persistent manual sign-in, per-account rate-limit controls, and a CAPTCHA browser handoff; manual validation remains required.
- 2026-07-19: R11 T2 P1 application pipeline UI — status tracking (interested/applied/interview/offer/rejected) on `JobApplication`, filter + badges on jobs list, sidebar card on job detail; 187 tests passing.
- 2026-07-20: Ad-hoc bug-fix detour (not a roadmap slice) prompted by live search use: fixed false-positive CAPTCHA detection in `linkedin.py`/`glassdoor.py` (was blind substring match on full page HTML, now requires challenge URL or visible widget, never fires if job listings already rendered); made location mandatory across all three search endpoints + frontend validation; added close/X buttons to the search-progress modal (search runs server-side via SSE regardless of modal state, so closing is safe). Diagnosed but did not fix: LinkedIn resolving plain-text "Armenia" to "Armenia, Quindío, Colombia" — user wants the fix to build on the now-mandatory location field rather than a server-side disambiguation guess; still open. Uncommitted.
- 2026-07-20: Two more ad-hoc fixes (not roadmap slices): added a "Job Search" button to the platform-accounts page (`accounts/list.html`) linking to `/jobs/search?candidate_id=`; diagnosed and cleared a stuck LinkedIn rate-limiter cooldown in `data/rate_limits.db` — the cooldown row (`linkedin:1`, set 16:38:54) predated the same-day CAPTCHA-detection fix (commit `2d2167f`, 17:11:13) and was a leftover false-positive block, not a live one. DB row deleted directly (gitignored, no code change). `accounts/list.html` change is uncommitted.
- 2026-07-20: Retired legacy LLM function mappings/selectors — document title parsing now uses the configured Document Analysis route; removed stale mapping UI/APIs and fixed the resulting startup import; 187 tests pass.
- 2026-07-20: AI provider templates — fixed immediate/apply-template form population, refreshed defaults from remote-rover/current provider guidance, and routed listed OpenAI-compatible templates through LiteLLM; focused tests and browser verification pass.
- 2026-07-21: AI-track planning capture (docs only, no code) — documented the agent subsystem + isolated agentic reason/act/observe loop (`docs/design/ai-agent-graph.md`, implemented-vs-planned), audited LLM latency/token best-practice gaps (`docs/design/ai-llm-efficiency-audit.md`), and reshaped roadmap Phase 6 into 6 vertical slices + added Phase E (7 LLM-efficiency slices) with a revisit trigger; deferred AI track, no implementation started.
- 2026-07-21: T2 P2 — per-job tailored resume/cover letter, reconciled by hand from worktree `agent-ad360b5d67189f2aa` (`e708346`) rather than plain-applied: ownership/migration groundwork was already on main so only the new `GeneratedDocument` model, `services/document_generation.py`, two `routes/jobs.py` endpoints, and the `jobs/detail.html` UI were transferred; the worktree's LLM call was rewired off the retired function-mapping system onto `resolve_chat_model_selection(purpose="document_analysis")`. 205/205 tests pass.
- 2026-07-21: T2 P3 — on-demand "re-run this search" (`POST /jobs/lists/{id}/rerun`) plus "N new since last run" diff on the saved-lists index and single-list view; diff is the existing `first_sighting` count per `docs/design/search-runs-and-lists.md`, no schema change. Scheduling/cron descoped (no scheduling infra in this codebase); this closes Phase 9's AFK checklist, T3 R3 HITL items remain.
- 2026-07-21: T3 R3 design pass (`docs/design/linkedin-login-reliability.md`) plus AFK sub-slices D1-D3: login-wall detection now persists `account.status="expired"` (mirrors existing CAPTCHA handling), `_search_linkedin` skips the live scrape when status is already `expired`/`captcha_required`, and the search-config platform cards surface the specific reason instead of a flat "Not Connected". 210/210 tests pass. D4 (pre-search probe button) remains, needs maintainer live LinkedIn validation.
- 2026-07-21: Roadmap cleanup — collapsed fully-complete Thread T1 and Thread T2 into short summaries in `roadmap.md` (every slice checked; detail lives in `progress.md`), trimmed the now-stale cross-thread-edge caveat. T3 R3 D4 implemented: "Check LinkedIn connection" button on the search-config page reuses the existing `/candidates/{id}/accounts/{id}/test` probe route (no new backend route); `platform_status` now carries `account_id`. 2 new tests in `test_linkedin_login_reliability.py`; 35/35 jobs+search tests pass. Still HITL — needs maintainer live LinkedIn validation before D4 is trusted.
- 2026-07-21: Phase 9 closed — maintainer completed basic live LinkedIn validation of D4's probe button, result looked correct; comprehensive testing deferred to later, non-blocking. Multi-platform expansion (WWR live implementation onward) deliberately deferred to new roadmap Phase F — current WWR adapter is fixture-stub only (live nav raises `NotImplementedError`), and the maintainer wants LinkedIn-only polish first plus a fresh design pass before more platforms. Opened Phase 9.5 (stabilization polish, LinkedIn-first) as the new active phase, per branch name `stabilize/cleanup-foundation`; first candidate slice is the location-default persistence fix (fixes the Armenia/Colombia LinkedIn location-ambiguity bug via UX persistence of the user's edited location, not server-side guessing or a new country field — write-back target still open).
- 2026-07-21: Phase 9.5 — Title-route profile isolation: applied `get_owned_candidate` to all 9 routes in `candidate_parser.py` (previously none checked ownership at all, including `reset-to-extracted` which had no candidate check whatsoever); added `test_job_title_route_ownership.py` (9 cross-profile-denial tests). 222/222 tests pass.
- 2026-07-21: Phase 9.5 — Preferred-title extraction quality: the profile/resume "fast path" in `job_title_parser.py` used to raw-dump every `experience[].title` as a curated preferred title, bypassing the concise target-role policy the LLM prompt enforces on the raw-file path. Unified both paths through one shared `_run_job_titles_prompt` helper so the same policy prompt judges structured-experience input as it does raw file text; added `test_job_title_extraction_policy.py` (2 tests). 224/224 tests pass.
- 2026-07-21: Phase 9.5 — Preferred-title review reliability: completed parse-review occurrence synchronisation, truthful save feedback, and the manual-add repair; added idempotent-resave and manual-add regression coverage. 226/226 tests pass.
- 2026-07-21: Phase 9.5 — Chat and settings shell polish (all 6 slices): provider registry Cancel-edit control + tightened save feedback (`settings/llm.html`); chat inline session rename (header + per-row) replacing `prompt()`, and Markdown-formatted `copyFullTranscript()`; trace/diagnostics `<details>` blocks persist open/closed state per block-kind via localStorage (`chat.html`); dark/light theme toggle persisted via localStorage (`base.html`, `app.js`, `style.css`); `GET/POST /api/ai-settings/export|import` validate the full document before writing so invalid imports never touch current settings (`routes/ai_settings.py`, tested); new `/settings/tools` page renders the existing `/api/ai/tools` registry. 229/229 tests pass; manually smoke-tested all new/changed pages return 200 via a local server run.
- 2026-07-21: Phase 10.1-10.4 — Shared LLM runtime foundation, jumped ahead of Phase 9.5 closure by maintainer request (deliberate sequencing call, not a hard blocker per roadmap ordering note). 10.1: Ollama calls now set `keep_alive=-1` in `_build_completion_kwargs` (`llm_service.py`), matching the fix and rationale already validated in the `remote-rover` reference project (`gcs_server/ai/provider_registry.py`) for the same 5-minute-idle-unload/20-40s-reload problem. 10.2: added a `max_output_tokens` field end-to-end — `ai_config_store.py` default provider schema, `RuntimeProvider` dataclass (`ai_routing.py`), `_build_completion_kwargs` cap application, and a matching `settings/llm.html` form field (mirrors the existing `context_window` field/pattern); no remote-rover precedent existed for this one (net-new). 10.3: verified only, no code change — LiteLLM already pools/reuses a persistent httpx client per provider via its internal `in_memory_llm_clients_cache`; confirmed by direct instantiation. 10.4: added a `json_mode` flag threaded through `_build_completion_kwargs`/`send_message`, probing `litellm.get_supported_openai_params` for `response_format` support and falling back silently to the existing prompt-only JSON extraction when unsupported or on any probe error; wired into skills extraction, document parsing, and job-title parsing (all three already ask for JSON in-prompt, satisfying OpenAI's json-mode requirement). No remote-rover precedent (net-new; that project doesn't use litellm or JSON mode at all). 239/239 tests pass (10 new: keep_alive, max_tokens cap presence/absence, json_mode on/off/fallback, config threading).
- 2026-07-22: Phase 9.5 — location-default persistence fix (last open AFK slice): resolved the HITL sub-decision as a dedicated field — added `CandidatePreferences.last_search_location` (`models/supporting.py`) plus `migrate_search_location` (`database.py`), a `_persist_search_location` helper in `routes/jobs.py` called from all three search-start endpoints (`/search`, `/search/start`, `/search/async`), and repointed the `GET` search-config handler to prefer it over `candidate.location`. `candidate.location` (the profile's home location) is never overwritten. Fixes the Armenia/Colombia location-ambiguity bug via UX persistence, not server-side guessing. Tests: `test_search_location_persistence.py` (2 new). 241/241 tests pass.
- 2026-07-22: Ad-hoc bug-fix detour (not a roadmap slice), prompted by live search use: added a "Platform Settings" shortcut link from each job-search platform card to the matching anchor on the platform-accounts page (`search_config_partial.html`, `accounts/list.html#platform-account-<platform>`) — the LinkedIn rate-limit/operating-hours controls already existed there and were already editable (including `0` to disable), the user just hadn't found the page. Separately fixed a real bug surfaced by a live 0-jobs run: `job_search.py`'s "why did we get 0 jobs" diagnostic re-called `check_rate_limiter()` after the scrape had already logged a request at "now", so it always falsely reported "skipped by rate limiter" regardless of the actual cause; replaced with the scraper (`linkedin.py`) recording its real skip/error reason (`rate_limited_reason` / `last_error`) for the orchestrator to read instead of re-guessing. Also fixed the likely actual root cause of that run's 0 jobs: `page.goto(..., wait_until="networkidle", timeout=60000)` was hanging the full 60s because LinkedIn's background traffic never goes idle; changed to `domcontentloaded` with a 30s timeout (job-card selector wait already covers readiness). 241/241 tests pass. Uncommitted.
- 2026-07-22: Same detour, round 2 — the `domcontentloaded` fix confirmed live: browser reached the real authenticated LinkedIn jobs-search results page (screenshot showed 43 real results), but the run then reported a false-positive CAPTCHA. Root cause: `_is_captcha`'s fallback `[id*='captcha' i]` DOM-presence check matched LinkedIn's hidden/dormant CAPTCHA scaffolding once its `.job-search-card` fast-path (written for the old guest/anonymous page markup) failed to match the authenticated SPA's actual DOM. Fixed by requiring the widget to be `:visible`, matching the method's own docstring contract. Added `_dump_debug_snapshot` (writes to gitignored `data/debug/`) so if `.job-search-card` still doesn't match on the next run, we get real markup instead of guessing selectors blind — working hypothesis, not yet confirmed, is that job extraction has never matched a logged-in session's actual DOM. 241/241 tests pass. Uncommitted.
- 2026-07-22: Same detour, round 3 — confirmed hypothesis using the maintainer's real `data/debug/linkedin_no_job_cards_20260722_004417.html` snapshot: LinkedIn's authenticated jobs-search DOM has fully replaced the old guest-page card structure the scraper targeted, so `_collect_jobs` always hit its `wait_for_selector` timeout and reported "No job listings found" even on a successful, logged-in load with real results present. Rewrote `linkedin.py` selectors against the captured markup: card container `div.job-card-container[data-job-id]` (new `JOB_CARD_SELECTOR` constant, also reused in `_is_login_wall`'s job-cards-present check), title from `a.job-card-list__title--link`'s `aria-label` (visible text is split across nested spans), company from `.artdeco-entity-lockup__subtitle`, location from the first `.job-card-container__metadata-wrapper li`, posted-date via a regex scan of card text (no longer a dedicated element — mixed in with "Viewed"/"Easy Apply" footer badges), and the click-through description pane from `.jobs-box__html-content` (was `.show-more-less-html__markup`). Verified all new selectors against the real captured snapshot with lxml (25 cards matched; title/company/location/href all extracted correctly) — not yet verified against a live session. 241/241 tests pass (1 pre-existing order-dependent flake in `test_search_location_persistence.py`, passes in isolation, unrelated to this change). Uncommitted.
- 2026-07-22: Same detour, round 4 — the round-3 selector rewrite worked live: the maintainer's next run collected 7 real jobs, surfacing three new issues. (1) All 7 jobs scored 0: title-similarity 0 is legitimate (candidate's active preferred titles are all SDET/QA/VLSI-focused, don't match "AI Engineer"-style results), but skills-overlap 0.0 is a real bug — every one of the candidate's 58 skill rows is `is_active=False`, traced to `routes/skills.py`'s overwrite-mode skills-parse *preview* endpoint, which deactivates all current active skills and commits immediately but only returns the newly-parsed list as an in-memory preview, never persisting it unless a separate confirm step runs; left unfixed pending maintainer direction (touches live profile data). (2) Job links were missing/broken: added external-link icons to `jobs/list.html` and `jobs/saved_list.html` (the latter had no per-job link at all); found `linkedin.py` was storing host-relative URLs (`/jobs/view/…`) instead of absolute ones (fixed at the source) and added a `Job.external_url` model property to normalize old rows; also found all three scrapers' `to_dict()` output was missing a `"platform"` key entirely, so every saved job's `platform` column silently defaulted to `"unknown"` — fixed all three call sites in `job_search.py` and corrected the 7 existing dev-DB rows directly. (3) Added a "Copy" button on `jobs/detail.html`'s description card (title/company/location/link + full description as plain text via clipboard, mirroring `chat.html`'s `copyFullTranscript()` pattern). 241/241 tests pass. Uncommitted.
- 2026-07-22: Housekeeping (not a roadmap slice) — renamed the project from `job-finder-qwen-code` to `job-finder-app` (drops the scaffolding-tool suffix; distinguishes this web app from the separate coding-agent-driven job-finder). Renamed the GitHub repo → `vardan6/job-finder-app` via `gh repo rename` (old URL auto-redirects), repointed the local `origin` remote (verified reachable), and updated all in-repo string references: `README.md` (title + layout), `docs/project-context.md` (layout), `history/IMPLEMENTATION_HISTORY.md` (repo URL), `history/handoff-remote-rover-gap-review-2026-05-31.md` and `history/SKILLS_MANAGER_COMPLETE_SUMMARY.md` (absolute paths). Untracked `.claude/worktrees/` copies left as-is. The local working directory itself is NOT yet renamed — must be done between sessions (`mv job-finder-qwen-code job-finder-app`, then reopen). Uncommitted.
- 2026-07-23: Third ad-hoc architecture-review pass (via `/improve-codebase-architecture`, not a roadmap slice) — surfaced 7 deepening candidates across job scraping/dedup, AI routing, and job analysis; implemented 5, declined 2 as over-abstraction after closer reading. Implemented: (1) shared `DEDUP_IDENTITY_FIELDS` constant in `job_deduplication.py` so `from_scraped_dict` and `calculate_similarity` can't drift on what defines a duplicate; (2) migrated `LinkedInJob`/`GlassdoorJob` to build the previously-unused `scrapers/base.py` `PlatformJob` adapter (added `salary`/`job_type` fields), resolving the first-pass "scraper duplication" deferred item; (3) extracted `_resolve_session_routing` shared by `/api/chat` and `/api/chat/stream` in `routes/chat.py`, removing duplicated mode-to-purpose mapping and error handling; (4) added a `Purpose` str-enum in `ai_capabilities.py` replacing raw routing-purpose string literals at 7+ call sites (needed a `__str__` override since Python 3.12 changed str-Enum formatting, which broke an error-message test); (5) replaced `analyze_job`'s 7 flattened candidate-* scalar params with one `CandidateProfile` object (`job_analysis.py`), resolving the second-pass "analyze_job shallow interface" deferred item — updated `job_search.py`'s call site and two test files (`test_job_analysis.py`, `test_remote_verification.py`) to the new signature. Declined: a per-provider adapter-registry for `_build_completion_kwargs` in `llm_service.py` (already a reasonably deep module for 2 special cases across 12 providers) and a shared prompt-assembly module between `job_analysis.py`/`document_generation.py` (only real overlap is a one-line truncation, not enough to justify a module). 255/255 tests pass. Uncommitted.
- 2026-07-23: Fourth ad-hoc architecture-review pass (via `/improve-codebase-architecture`) — verified the 6 candidates left unscheduled by the first two passes still held in current code (report: `/tmp/architecture-review-20260723-225816.html`, not committed), then implemented the maintainer's pick: `JobSearchService.search()` (`services/job_search.py`) now owns the global search-lock precondition itself instead of trusting callers. Previously three `routes/jobs.py` handlers (`/search`, `/search/start`, `/search/async`) were each supposed to call `is_search_running()` before invoking the service — but `/search/async` never actually did, a real enforcement gap. Renamed the existing method body to `_execute_search`; the new thin `search()` does a non-blocking `get_search_lock().acquire()`, returns `SearchResult(success=False, errors=[...])` immediately if held (fail-fast, matching prior route UX — no polling/waiting), and releases the lock in a `finally` regardless of outcome. Removed the now-redundant `is_search_running()` checks from the two routes that had them (their existing `result.errors` rendering — HTML warning banner and SSE done-event log line — already display the rejection message unchanged), and deleted `search_lock.is_search_running()` itself since nothing calls it anymore. Added `tests/test_job_search_lock.py` (3 tests: rejects when held, releases after success, releases after an uncaught exception). Remaining 5 candidates from the backlog (dual LLM provider-config storage completing ADR-0001, `_execute_search` is still a large method, `routes/jobs.py`'s triplicated search-start preamble — largely resolved as a side effect of this change but not independently reviewed, search-run diagnosis spread across 4 state channels, splitting `job_title_parser.py` into extraction vs. provenance modules) remain open, undecided, not yet ADR'd. 258/258 tests pass. Uncommitted.
- 2026-07-24: Fifth ad-hoc architecture-review pass (via `/improve-codebase-architecture`), scoped to the job-intake pipeline (`job_search.py` + its scraper and dedup seams) via an `Explore` subagent survey; implemented the top 2 candidates it surfaced, chosen for sharing that pipeline context. (1) Unified scraper session-outcome reporting: added `ScraperSessionState` (`scrapers/base.py`) as the one place defining the `manual_challenge_handoff`/`login_wall_detected`/`rate_limited_reason`/`last_error` contract; both `LinkedInScraper` and `GlassdoorScraper` now inherit it instead of each hand-rolling the same four attributes. This surfaced a real bug: `GlassdoorScraper` never set any of them, so a Glassdoor CAPTCHA or login wall silently never updated `PlatformAccount.status` (LinkedIn's had for a while) — fixed by having Glassdoor report outcome the same way, and by collapsing `job_search.py`'s LinkedIn-only inline status-update block into one `_apply_scraper_outcome` helper used by both `_search_linkedin` and `_search_glassdoor`. Documented the convention in `docs/design/platform-adapters.md`. (2) Deleted `job_deduplication.py`'s dead, independently-reimplemented duplicate-detection path: `check_duplicate_in_db`, `find_duplicates`, and `get_duplicate_report` had zero production callers and zero test coverage — a second fuzzy-matching implementation that could silently drift from the `JobDeduplicator.calculate_similarity` path `job_search.py` actually uses; also dropped the now-unused `Session` import. Declined/deferred candidates from this pass (not implemented): three near-duplicate search-trigger endpoints in `routes/jobs.py` (`/search`, `/search/start`, `/search/async` — the async one appears to not persist results), and duplicated `CandidateProfile`-construction logic between `job_search.py` and `job_analysis.py`. 258/258 tests pass. Uncommitted.
- 2026-07-24: Phase 9.5 closed (maintainer decision, no new code) — the remaining "Broader UX polish pass on the LinkedIn-only search/results/apply flow" checklist item was closed without a separate dedicated UI pass; the maintainer judged it covered by the cumulative Phase 9/9.5 work already landed (profile-page card pattern, results-table upgrade, application pipeline UI, chat/settings shell polish, location persistence, preferred-title reliability) plus the five ad-hoc `/improve-codebase-architecture` reliability passes on the scraper/dedup/search-lock path. `roadmap.md` updated accordingly; 258/258 tests re-verified passing (unchanged). The fifth architecture-review pass above remains uncommitted.
- 2026-07-24: Seventh ad-hoc architecture-review pass (via `/improve-codebase-architecture`, not a roadmap slice), scoped to the scraper seam — directly continues the sixth pass, which had deepened the *session lifecycle* but left the *per-card surface* shallow and duplicated across `LinkedInScraper`/`GlassdoorScraper`. Implemented one coherent cluster of three related deepenings, all in the Playwright-adapter subsystem. (1) Declarative block-detection (the payoff): `_is_login_wall`/`_is_captcha` were four structurally-identical ~20-line methods (two per adapter), reachable only through a live browser — and because the existing base-test stub overrides them wholesale, the *real* detection logic was untested. Each adapter now declares its block signatures as class-attribute *data* (`login_wall_url_tokens`, `login_wall_credential_selectors`, `captcha_url_tokens`, `captcha_widget_selector`, `results_present_selector`), and `PlaywrightJobScraper` evaluates them with one fixed precedence rule (challenge URL blocks outright; else rendered results mean the page is fine; else a present widget counts). Behavior preserved exactly, including LinkedIn's `:visible` widget requirement vs. Glassdoor's plain selectors. (2) Shared per-card description fetch: the identical click→wait→read→snippet→hash dance (~25 lines each) moved into `PlaywrightJobScraper._load_job_description`, driven by a per-platform `description_selector`/`description_click_delay`. (3) `_text`/`_has` DOM-read helpers on the base kill the repeated `(await el.inner_text()).strip() if el else ""` idiom (Glassdoor's field block went ~28 lines → 6). New `tests/test_scraper_block_detection.py` drives the *real* LinkedIn/Glassdoor declarations against a fake page exposing only `url`+`query_selector` (10 tests: URL-token blocking, the both-credential-fields rule, rendered-results-beat-dormant-widget) — first-ever coverage of that detection logic. Updated `docs/design/platform-adapters.md`'s adapter section to record the declarative-detection seam. Net −807/+309 across the two adapters. Full suite 274/274 pass (was 264; +10). Uncommitted. Declined/deferred (unchanged): three near-duplicate search-trigger endpoints in `routes/jobs.py`, duplicated `CandidateProfile`-construction between `job_search.py`/`job_analysis.py`.
- 2026-07-24: Sixth ad-hoc architecture-review pass (via `/improve-codebase-architecture`, not a roadmap slice), scoped to the scraper seam — the same job-intake pipeline as the fifth pass, chosen because it's the codebase's hot spot and this directly continues the fifth pass's `ScraperSessionState` work. Finding: `LinkedInScraper` and `GlassdoorScraper` each re-implemented the *entire* Playwright session lifecycle (global-lock acquire/release, rate-limit gate, browser launch, navigate-with-delays, login-wall/CAPTCHA dispatch, collect/scroll loop, cookie save, success logging, uniform error capture) — two adapters duplicating it = a real seam, and because both existing tests monkeypatch `search_jobs` wholesale, that orchestration body was effectively untested (exactly where the recent CAPTCHA/selector bug fixes landed). Deepening: new `backend/scrapers/playwright_base.py` `PlaywrightJobScraper(ScraperSessionState)` owns the whole session skeleton once (`_run_search_session` + shared `_collect_jobs`/`_scroll_to_load_more`/`_human_delay`); kept out of `base.py` so the fixture-only WWR adapter stays import-light. LinkedIn and Glassdoor became thin adapters declaring only class attributes (URL, selectors, nav/scroll/delay tuning) + hooks (`_build_search_url`, `_extract_job_from_card`, `_is_login_wall`, `_is_captcha`, `_on_captcha` policy — Glassdoor cooldown vs. LinkedIn manual handoff, LinkedIn-only `_on_no_cards` debug snapshot); each keeps its own thin `search_jobs` so LinkedIn's extra params (`rate_limit_scope`/`rate_limit_settings`/manual-handoff keys) stay explicit. Behavior preserved exactly except one deliberate strictly-additive change: Glassdoor now also emits a "…: search failed — {e}" progress line on error, matching LinkedIn. New `tests/test_playwright_scraper_base.py` drives the template with fake page/manager/pool objects (happy path, rate-limit gate, login wall, CAPTCHA hook, navigation error, empty results) — first-ever coverage of that orchestration. Updated `docs/design/platform-adapters.md` with a "Shared browser session lifecycle" section. Net: three ~460-line scrapers → 287-line deep base + two adapters carrying only platform-specific markup/detection. Full suite 264/264 pass (was 258; +6 new). Uncommitted. Declined/deferred (unchanged from fifth pass): three near-duplicate search-trigger endpoints in `routes/jobs.py`, duplicated `CandidateProfile`-construction between `job_search.py`/`job_analysis.py`.
- 2026-07-24: Eighth ad-hoc architecture-review pass (via `/improve-codebase-architecture`, not a roadmap slice) — implemented the `CandidateProfile`-construction candidate that the fifth/sixth/seventh passes had repeatedly deferred, then committed the whole uncommitted seam+profile backlog. Finding: six read-sites each re-derived "which of a candidate's skills/titles count" with drifted predicates — and the analysis-profile build (`job_search.py`) had lost the skill `is_active` check every other site enforced, so soft-deleted skills leaked into the LLM job-analysis prompt (a real correctness bug, not just duplication). Deepening: added canonical `Candidate.active_skill_names()` / `active_titles()` accessors (`models/candidate.py`, the natural home for the predicate) + a `CandidateProfile.from_candidate(candidate)` seam (`services/job_analysis.py`), and routed all non-scraper sites through them — `job_search.py` (profile build + `_create_job_record` scoring), `job_llm_refinement.py` (`profile_fingerprint` + `_profile_text`), `routes/chat.py` (context blocks), `routes/jobs.py` (skills-count info, now truthful to what analysis uses). New `tests/test_candidate_active_profile.py` (3 tests) pins the predicate and the leak fix. 277/277 pass (was 274; +3). Then committed the previously-uncommitted work as two logical commits on `stabilize/cleanup-foundation`: `9775295` (this profile refactor — helper/definition files) and `cf8aff2` (the fifth–seventh scraper-seam passes, carrying `job_search.py` since its profile call-sites can't be separated from the scraper-outcome changes in the same file). Not pushed. Declined/deferred (unchanged): three near-duplicate search-trigger endpoints in `routes/jobs.py`; dual LLM provider-config storage; `_execute_search` still large; search-run diagnosis across 4 state channels; splitting `job_title_parser.py`.
