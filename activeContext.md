# Active Context

- Mode: implementation (Phase 9 product completion)
- Phase: 9 — three parallel threads, branch `stabilize/cleanup-foundation`
- State: T1, T2 R7/P1, and Phase-10 R9 are complete; T3 R3 still needs maintainer validation. WWR is selectable but policy-gated.
- Next atomic step: reconcile T2 P2 (tailored resume/cover letter) from `worktree-agent-ad360b5d67189f2aa` (`e708346`) selectively: preserve ownership/migration groundwork from `9d4e67b`, transfer only P2 document-generation work, and hand-merge shared jobs/detail files. Do not plain-apply its commit.
- Next after that: T2 P3 scheduled re-search with "N new since last run" diff, building on saved lists (unblocked now that P1 is merged) — do this as its own slice, not parallelized. T3 R3 HITL (LinkedIn CAPTCHA trial, WWR authorization) also remains open whenever maintainer is available.
- Blockers/env: Do not tick T3 R3 until the maintainer manually validates LinkedIn and authorizes/validates WWR live access; its current WWR path fails closed without provider access. Use `./job-finder-web/venv/bin/python -m pytest -s ...`; local `python3` lacks `pytest`. P2 worktree at `.claude/worktrees/agent-ad360b5d67189f2aa` must stay until reconciled; P1 worktree at `.claude/worktrees/agent-a459fce5da360d96d` is merged and safe to remove.
- Open questions: none from Phase 9 planning. New, from this session's bug-fix detour: how should "Armenia" (country) be disambiguated for LinkedIn's location search so it doesn't resolve to the Colombian city of the same name — user wants a fix building on the now-mandatory location field, not a silent server-side guess (e.g. auto-appending "Yerevan,").
- Deferred AI track (2026-07-21, planning only): roadmap Phase 6 now decomposed into vertical slices 6.1–6.6 and a new Phase E (E.1–E.7 LLM efficiency) exist; design canon in `docs/design/ai-agent-graph.md` + `ai-llm-efficiency-audit.md`. Two maintainer decisions pending: promote Tier-1 efficiency (E.1–E.3: Ollama `keep_alive`, `max_tokens`, client reuse) above Phase 9 to fix current LLM latency? and E.4 prompt-cache prefix design when picked up. Not active until Phase 9 closes or re-prioritized.
- Discarded as noise: Full rewrite (rejected); doing provenance before card unification (rework); scheduling new tables before Q8 groundwork (double migration); parallelizing P1+P3 together (both touch models/job.py + routes/jobs.py, would collide).
- Uncommitted, outside roadmap (2026-07-20): fixed CAPTCHA false-positive detection (linkedin.py, glassdoor.py), made location mandatory on all 3 search endpoints + frontend, added close/X to search-progress modal, added a "Job Search" button to `accounts/list.html`, retired legacy LLM function mappings/selectors so document parsing uses the Document Analysis route from AI Settings, and fixed AI Settings provider-template application/defaults plus OpenAI-compatible provider routing. Files: `job-finder-web/backend/routes/jobs.py`, `job-finder-web/backend/scrapers/{linkedin,glassdoor}.py`, `job-finder-web/frontend/templates/jobs/search_config_partial.html`, `job-finder-web/frontend/templates/accounts/list.html`, and the AI-routing/settings cleanup files in `git status`. Also cleared a stale LinkedIn rate-limiter cooldown directly in `data/rate_limits.db` (gitignored, no code change) — it predated the CAPTCHA fix and was a leftover false-positive block. See `progress.md` 2026-07-20 entries. Decide whether to commit the file changes before starting the T2 P2 merge above.
