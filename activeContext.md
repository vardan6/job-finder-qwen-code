# Active Context

- Mode: stabilization closed out — Phase 9 and Phase 9.5 are both now complete on branch `stabilize/cleanup-foundation`.
- State: Phase 9 and 9.5 both closed. This session ran an eighth ad-hoc `/improve-codebase-architecture` pass (not a roadmap slice): centralized candidate active-skill/title derivation into `Candidate.active_skill_names()`/`active_titles()` + `CandidateProfile.from_candidate()`, routing all six drifted read-sites through them and fixing a real leak (soft-deleted skills reaching the analysis prompt). Then committed the whole prior backlog as two logical commits on `stabilize/cleanup-foundation`: `9775295` (profile refactor) and `cf8aff2` (fifth–seventh scraper-seam passes). 277/277 tests pass (`./job-finder-web/venv/bin/python -m pytest job-finder-web/tests -q`). Full detail in `progress.md` (2026-07-24 eighth-pass bullet). Working tree now clean except these state files. Branch ahead of `origin` by 3, not pushed.
- Next atomic step: pick the next unchecked slice via `/next-slice` — first candidate is Phase 10.5 HITL (per-model verified tool-calling capability gate + visible fallback; contract `docs/design/agent-provider-capabilities.md`), but Phase 11's platform-design HITL review is equally eligible since both phases are unblocked now that 9.5 is closed. (Push the 3 commits when the maintainer is ready.)
- Blockers/env: use `./job-finder-web/venv/bin/python -m pytest job-finder-web/tests -q` (run from repo root — pointing pytest at `backend/` alone finds 0 tests); local `python3` lacks `pytest`.
- Still pending from prior sessions (untouched this session): restoring the candidate's 58 already-deactivated skill rows (re-parse from resume now that overwrite is safe, or another source of truth) — touches live profile data, needs maintainer direction. Project directory rename `job-finder-qwen-code` → `job-finder-app` still needs `mv` + reopen between sessions if not already done.
- Open questions: whether the LinkedIn selectors hold across more live runs; whether posted-date regex scanning is robust long-term; how the maintainer wants to restore/re-activate skills.
- Deferred: Phase 10.5+ agentic runtime needs its HITL capability-gate decision; Phase 11 needs a platform design pass; Phase 12 accounts remains end-state. Undecided architecture-review backlog (see `progress.md` for the five-pass history), not scheduled: dual LLM provider-config storage (`llm_config.py` DB-backed vs `ai_config_store.py`/`ai_routing.py` JSON-backed — completes ADR-0001), `_execute_search` is still a large method, search-run diagnosis spread across 4 state channels, splitting `job_title_parser.py` into extraction vs. provenance-reconciliation modules, three near-duplicate search-trigger endpoints in `routes/jobs.py` (`/search`/`/search/start`/`/search/async` — the async one appears to not persist results). (The `CandidateProfile`-construction duplication was implemented in the eighth pass — done.)
- Discarded as noise: no silent Armenia/Colombia location guess or new country field; preferred-title review-save persists in its route-level regression, so the remaining live risk is stale app code or model output needing review, not SQLite restart persistence. Third-pass architecture review (2026-07-23) declined 2 candidates as over-abstraction: a per-provider adapter-registry for `llm_service.py`'s `_build_completion_kwargs` (already reasonably deep for 2 special cases across 12 providers) and a shared prompt-assembly module between `job_analysis.py`/`document_generation.py` (only real overlap is a one-line truncation). The three near-duplicate search-trigger endpoints remain the main declined/deferred candidate left for a future pass.
