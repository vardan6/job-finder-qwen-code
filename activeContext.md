# Active Context

- Mode: implementation — Phase 9.5 stabilization polish; branch `stabilize/cleanup-foundation`.
- State: LinkedIn live search collects jobs with current authenticated-DOM selectors; URL/platform persistence and result-page controls repaired. Preferred-title parsing uses the concise target-role prompt with an endpoint-level review-save persistence regression. `routes/skills.py`'s overwrite-mode parse-preview no longer silently deactivates skills before confirmation (2026-07-22). Four ad-hoc `/improve-codebase-architecture` passes on 2026-07-23 progressively deepened `job_analysis.py`, `job_deduplication.py`, `routes/chat.py`/`ai_capabilities.py`, and — most recently — `JobSearchService`: `search()` now owns the global search-lock precondition itself (fail-fast, non-blocking, `finally`-released) instead of three `routes/jobs.py` handlers each trusting themselves to check it first (one, `/search/async`, never actually did — a real gap, now closed); body renamed to `_execute_search`; dead `search_lock.is_search_running()` deleted. Full detail on all four passes is in `progress.md`. 258/258 tests pass. The working tree is uncommitted and spans prior sessions.
- Next atomic step: HITL — decide how to restore the candidate's 58 already-deactivated skill rows (re-parse from resume now that overwrite is safe, or another source of truth); this touches live profile data.
- Next after: scope the remaining Phase-9.5 LinkedIn search/results/apply UX pass via `/next-slice` — still unscoped, needs maintainer input on what "polish" means here.
- Blockers/env: use `./job-finder-web/venv/bin/python -m pytest -s ...`; local `python3` lacks `pytest`. Re-parse and save preferred titles once in the running app (restart it first if it predates the current uncommitted code).
- Pending rename: project renamed `job-finder-qwen-code` → `job-finder-app` (GitHub repo + local remote + in-repo references done 2026-07-22); the local working directory still needs `mv job-finder-qwen-code job-finder-app` between sessions, then reopen in the new path.
- Open questions: whether the LinkedIn selectors hold across more live runs; whether posted-date regex scanning is robust long-term; how the maintainer wants to restore/re-activate skills.
- Deferred: Phase 10.5+ agentic runtime needs its HITL capability-gate decision; Phase 11 needs a platform design pass; Phase 12 accounts remains end-state. Undecided architecture-review backlog (see `progress.md` for the four-pass history), not scheduled: dual LLM provider-config storage (`llm_config.py` DB-backed vs `ai_config_store.py`/`ai_routing.py` JSON-backed — completes ADR-0001), `_execute_search` (formerly `JobSearchService.search()`) is still a large method, search-run diagnosis spread across 4 state channels, splitting `job_title_parser.py` into extraction vs. provenance-reconciliation modules. Resolved by the fourth pass: search-lock enforcement moved into the service; `routes/jobs.py`'s triplicated search-start preamble shrank as a side effect (not independently reviewed for full dedup).
- Discarded as noise: no silent Armenia/Colombia location guess or new country field; preferred-title review-save persists in its route-level regression, so the remaining live risk is stale app code or model output needing review, not SQLite restart persistence. Third-pass architecture review (2026-07-23) declined 2 candidates as over-abstraction: a per-provider adapter-registry for `llm_service.py`'s `_build_completion_kwargs` (already a reasonably deep module for 2 special cases across 12 providers) and a shared prompt-assembly module between `job_analysis.py`/`document_generation.py` (only real overlap is a one-line truncation, not enough duplication to justify a module).
