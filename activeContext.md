# Active Context

- Mode: implementation — Phase 9.5 stabilization polish; branch `stabilize/cleanup-foundation`.
- State: LinkedIn live search now collects jobs with current authenticated-DOM selectors; URL/platform persistence and result-page controls were repaired. Preferred-title parsing uses the concise target-role prompt, filters bare generic model output, has an endpoint-level review-save persistence regression, and treats generic-only output as a successful empty review rather than a false 400. `routes/skills.py`'s overwrite-mode parse-preview no longer silently deactivates existing skills before user confirmation (fixed 2026-07-22; deactivation now happens in `save-parsed`, gated on `parse_mode`). `services/job_analysis.py`'s job-match prompt no longer hardcodes one candidate's profile — it's templated per-candidate from `location`/`timezone`/`experience_years`/`current_role`/active job titles, with the on-disk cache key extended to match (2026-07-23, ad-hoc, uncommitted). The working tree is uncommitted and spans prior sessions.
- Next atomic step: HITL — decide how to restore the candidate's 58 already-deactivated skill rows (re-parse from resume now that overwrite is safe, or another source of truth); this touches live profile data.
- Next after: scope the remaining Phase-9.5 LinkedIn search/results/apply UX pass via `/next-slice` — still unscoped, needs maintainer input on what "polish" means here.
- Blockers/env: use `./job-finder-web/venv/bin/python -m pytest -s ...`; local `python3` lacks `pytest`. Re-parse and save preferred titles once in the running app (restart it first if it predates the current uncommitted code).
- Pending rename: project renamed `job-finder-qwen-code` → `job-finder-app` (GitHub repo + local remote + in-repo references done 2026-07-22); the local working directory still needs `mv job-finder-qwen-code job-finder-app` between sessions, then reopen in the new path.
- Open questions: whether the LinkedIn selectors hold across more live runs; whether posted-date regex scanning is robust long-term; how the maintainer wants to restore/re-activate skills.
- Deferred: Phase 10.5+ agentic runtime needs its HITL capability-gate decision; Phase 11 needs a platform design pass; Phase 12 accounts remains end-state. 4 undecided architecture-review candidates from 2026-07-23 (see `progress.md`), not scheduled: dual LLM provider-config systems (`llm_config.py` DB-backed vs `ai_settings.py`/`ai_routing.py` JSON-backed — completes ADR-0001, not yet done), `JobSearchService.search()` untested god-method, LinkedIn/Glassdoor scraper duplication (unused `scrapers/base.py` adapter), `routes/jobs.py` triplicated search-start handlers.
- Discarded as noise: no silent Armenia/Colombia location guess or new country field; preferred-title review-save persists in its route-level regression, so the remaining live risk is stale app code or model output needing review, not SQLite restart persistence.
