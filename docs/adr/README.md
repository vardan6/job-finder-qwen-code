# ADRs

Architecture Decision Records capture decisions that are currently treated as
settled enough to guide implementation.

- `0001-ai-foundation-source-of-truth.md` — adopt a copied-and-adapted
  `remote-rover` AI foundation as the canonical AI architecture for
  `job-finder`
