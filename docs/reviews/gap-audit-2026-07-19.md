# Gap Audit — product-vision R1–R7 vs implementation (2026-07-19)

Method: targeted code inspection per requirement. Rating: OK / PARTIAL / GAP.
Drives Phase 9 slice ordering. Findings reference code as of
`stabilize/cleanup-foundation` @ bb1b83d.

| # | Requirement | Verdict | Evidence / gap |
|---|-------------|---------|----------------|
| R1a | Multi-file upload per candidate | OK | `routes/documents.py` upload with hash-dedupe, 10MB cap, .md/.txt/.pdf |
| R1b | File-type classification (resume/CV/other) | PARTIAL | `detect_document_type()` in `routes/documents.py:49` is **filename-keyword matching only** — content is never inspected; misnamed files misclassify. Vision implies content-aware detection (likely LLM-assisted) |
| R1c | Per-file titles/skills extraction | OK | `document_parser.py` + per-type parse prompts (`DocumentParsePrompt`), `source_document_id` links results to files |
| R1d | Manual edit after extraction | OK | title/skill edit flows exist (`skills.py`, detail page edit modes) |
| R1e | Extracted-vs-edited provenance | GAP | No `source: extracted\|edited` field on `PreferredTitle`/`CandidateSkill` (`models/supporting.py`); design in `docs/design/scoring-and-provenance.md` not implemented |
| R2 | Consistent profile-page cards | GAP (confirmed) | `candidates/detail.html`: cards diverge — plain headers (l.46, 81) vs click-to-collapse headers (l.114, 317, 391); job-titles card has bespoke edit/save/cancel button machinery others lack; inline `onclick` everywhere; no shared card component/macro |
| R3a | LinkedIn/Glassdoor login | PARTIAL | Login = manual cookie/storage-state paste (`platform_accounts.py`) + browser-manager assisted login; known unreliable; cookie expiry handling unclear |
| R3b | Login status UI (per-platform state, failure reasons, re-login) | PARTIAL | `status` column exists ("active") but no failure-reason surfacing or guided re-login flow |
| R3c | Cheap platform extensibility | PARTIAL | scrapers are two hand-written modules (`linkedin.py`, `glassdoor.py`) with no shared scraper interface |
| R4a | Search by curated titles | OK | `job_search.py` searches per-platform with title queries, dedupes (`job_deduplication.py`) |
| R4b | Platform research doc | GAP | nothing in `docs/research/` about platform ranking |
| R5 | Two-stage composite scoring | PARTIAL | LLM `skill_match_score` + `remote_score` exist (`job_analysis.py`), cached. But: no deterministic title-similarity/skills-overlap composite, no config weights, LLM runs per-job not top-N, no score breakdown UI. Design doc not implemented. Note: `calculate_armenia_compatibility()` hardcodes a personal location rule — should move to preferences config |
| R6 | Remote-status verification w/ contradiction evidence | PARTIAL | LLM prompt scores remote compatibility 0–100 + `remote_type`; but no explicit attribute-vs-description contradiction check and no quoted evidence output; `min_ai_remote_score` preference exists and is used as filter |
| R7a | Rich results table (salary, verified remote, score columns; sortable; best-match sort) | PARTIAL | `jobs/list.html` table has Title/Company/Location/Platform/RemoteScore/Status; **no salary column, no composite score, no column sorting, no best-match default order** |
| R7b | Saveable named search lists (date + detail), multiple lists | GAP | No search-run/list model exists (`models/job.py`); results accumulate into one jobs pool per candidate; searches are not saveable or revisitable as lists |

## Priority reading (worst → least)

1. **R7b saved search lists** — pure gap, user-visible, needs a `SearchRun` model.
2. **R2 card consistency** — confirmed maintainer complaint; fix via one shared
   card macro/component then migrate cards one per slice. Maintainer note
   (2026-07-19): needs a **dedicated behavior-level card review first** —
   multiple bugs and inconsistencies exist between similar-behavior cards
   (fold/unfold, file opening, listing, edit flows), beyond the structural
   divergence noted above.
3. **R1e provenance** — designed, unimplemented.
4. **R5/R6** — foundation exists (LLM job analysis); needs the decided
   two-stage composite, contradiction-evidence prompt, and breakdown UI.
5. **R3 login reliability** — hardest; needs its own design pass (session
   validity probing, expiry detection, guided re-login).
6. **R1b content-aware file classification, R4b research doc** — small.
